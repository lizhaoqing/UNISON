# Core deep learning
torch>=2.6.0
torchaudio>=2.6.0
numpy>=1.26.0
einops>=0.8.0

# HuggingFace ecosystem
transformers>=4.56.0
accelerate>=0.34.0
diffusers>=0.35.0
safetensors>=0.4.5
huggingface-hub>=0.34.0

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0
openai-whisper>=20240930    # used for auto-transcribing reference audio in zero-shot TTS

# Attention kernels — optional but strongly recommended.
# Without flash-attn the model automatically falls back to PyTorch SDPA (slower, more memory).
# Must be installed separately to match your CUDA/PyTorch version:
#
#   Option A — build from source (~10 min):
#     pip install flash-attn --no-build-isolation
#
#   Option B — prebuilt wheel (fast), find the right one at:
#     https://github.com/Dao-AILab/flash-attention/releases
#   e.g. for torch 2.6 + CUDA 12.4 + Python 3.10:
#     pip install https://github.com/Dao-AILab/flash-attention/releases/download/v2.7.4/flash_attn-2.7.4+cu124torch2.6cxx11abiFALSE-cp310-cp310-linux_x86_64.whl

# Utilities
pyyaml>=6.0
omegaconf>=2.3.0
loguru>=0.7.0
tqdm>=4.67.0
